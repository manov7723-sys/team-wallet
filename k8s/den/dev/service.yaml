apiVersion: v1
kind: Service
metadata:
  name: team-wallet
  namespace: den
  labels:
    app.kubernetes.io/name: team-wallet
    app.kubernetes.io/managed-by: deepagent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: team-wallet
  ports:
    - name: http
      port: 80
      targetPort: 3000
