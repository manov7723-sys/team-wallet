apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: team-wallet-jupiter-api-key
  namespace: den
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: deepagent-aws
    kind: ClusterSecretStore
  target:
    name: app-env
    creationPolicy: Merge
  data:
    - secretKey: JUPITER_API_KEY
      remoteRef:
        key: teams-dev/den/JUPITER_API_KEY
