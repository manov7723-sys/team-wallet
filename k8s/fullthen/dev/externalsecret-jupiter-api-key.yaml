apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: team-wallet-jupiter-api-key
  namespace: fullthen
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: deepagent-aws
    kind: ClusterSecretStore
  target:
    name: app-env
    creationPolicy: Merge
  data:
    - secretKey: JUPITER_API_KEY
      remoteRef:
        key: nodetoday-dev/fullthen/JUPITER_API_KEY
